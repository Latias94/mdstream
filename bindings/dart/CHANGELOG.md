# Changelog

## 0.4.0

- Added framework-neutral Dart FFI bindings for native mdstream engine and
  reducer sessions.
- Added typed readonly state views, stable node identity, explicit snapshot
  recovery, processor artifact leases, and lossless input batching.
- Added checked ABI layouts, structured errors, exact-once native ownership,
  and host-supplied dynamic-library loading.
