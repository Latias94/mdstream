{
  "name": "mdstream-workspace",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": "24.x",
    "pnpm": "11.9.0"
  },
  "scripts": {
    "wasm:check": "cargo +1.85.0 check -p mdstream-wasm --target wasm32-unknown-unknown --all-features",
    "wasm:test": "RUSTUP_TOOLCHAIN=1.85.0 wasm-pack test --node mdstream-wasm",
    "wasm:build:web": "RUSTUP_TOOLCHAIN=1.85.0 wasm-pack build mdstream-wasm --target web --profile wasm-size --no-pack --out-name mdstream_wasm --out-dir ../target/mdstream-wasm-pkg && node scripts/optimize-wasm.mjs",
    "wasm:build:node": "RUSTUP_TOOLCHAIN=1.85.0 wasm-pack build mdstream-wasm --target nodejs --dev --no-pack --out-name mdstream_wasm --out-dir ../target/mdstream-wasm-node",
    "web:prepare": "pnpm --filter @mdstream/core build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "artifacts:check": "pnpm build && node bindings/typescript/scripts/measure-artifacts.mjs --check"
  }
}
