#![allow(unused_imports)]

use mdstream::{
    BoundaryPlugin, CodeFenceHeader, Options, PendingTransformer, TerminatorOptions,
    is_code_fence_closing_line, is_list_marker_line_prefix, parse_code_fence_header,
    terminate_markdown,
};

fn main() {}
